<nav class="">
<h1>~ Five Step Goals ~</h1>
  <ul>
    <li> <%= link_to "Goals Home", root_path, :class => "header_links" %> </li>
    <% if user_signed_in? %>
      <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete, :class => "header_links"  %></li>
      <li><%= link_to "Profile Page", user_path(current_user), :class => "header_links"  %></li>
    <% else %>
      <li><%= link_to 'Sign In', new_user_session_path, :class => "header_links"  %></li>
      <li><%= link_to 'Sign Up', new_user_registration_path, :class => "header_links"  %></li>
    <% end %>
  </ul>
</nav>
